interface Register{
    name: string;
      roll: string;
      email: string;
      phone: string;
      reason: string;
      projects: string;
}

interface Update{
  scannedData: string;
      rating: number;
      status: string;
      remarks: string;
      panelName:string;
}
